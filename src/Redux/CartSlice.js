import { createSlice } from "@reduxjs/toolkit";
export const CartSlice = createSlice({
    name: "CartSlice",
    initialState: {basket:[]},
   
    reducers: {
          
         
          
           
           
          

    }
})
export const {SetTobasket} = CartSlice.actions
export default CartSlice.reducer